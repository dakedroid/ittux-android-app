<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:bottomLeftRadius="@dimen/keylines"
        android:bottomRightRadius="@dimen/keylines"
        android:radius="@dimen/keylines"
        android:topLeftRadius="@dimen/keylines"
        android:topRightRadius="@dimen/keylines" />
    <solid android:color="@android:color/white" />
    <stroke
        android:width="1dip"
        android:color="@color/colorAccent" />
</shape>